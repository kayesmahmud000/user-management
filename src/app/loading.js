import Loader from '@/components/Loader';
import React from 'react';

const loading = () => {
    return (
        <div className=''>
            <Loader/>
        </div>
    );
};

export default loading;